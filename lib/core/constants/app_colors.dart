import 'package:flutter/material.dart';

class AppColors {
  // الألوان الأساسية
  static const Color primary = Color(0xFF2563EB); // أزرق أغمق شوية
  static const Color primaryDark = Color(0xFF1E40AF);
  static const Color secondary = Color(0xFFF59E0B); // برتقالي
  static const Color accent = Color(0xFF10B981); // أخضر
  static const Color surface = Colors.white;
  static const Color textHint = Color(0xFF94A3B8);

  // ألوان حالة
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF97316); // برتقالي أغمق
  static const Color info = Color(0xFF3B82F6);

  // ألوان النص (لتحسين التباين)
  static const Color textPrimary = Color(0xFF0F172A); // أسود داكن جداً
  static const Color textSecondary = Color(0xFF475569); // رمادي داكن
  static const Color textLight = Color(0xFF64748B); // رمادي فاتح

  // ألوان الخلفية
  static const Color backgroundLight = Color(0xFFF8FAFC);
  static const Color backgroundDark = Color(0xFF121212);
}